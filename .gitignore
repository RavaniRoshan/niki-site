node_modules
dist
*Zone.Identifier
niki-product-studio.html

# env / secrets (keep .env.example tracked as a template)
.env
.env.local
.env.*.local

# Agent working files (local only, not in repo)
.kilo/
research/
